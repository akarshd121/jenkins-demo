pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the complex project'
                // Add build steps here
            }
        }
        stage('Test') {
            steps {
                echo 'Running all tests'
                // Add test steps here
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the complex project'
                // Add deployment steps here
            }
        }
    }
}
